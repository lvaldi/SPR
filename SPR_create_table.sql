CREATE TABLE MENTOR (
ID SERIAL,
Name VARCHAR(20) NOT NULL,
PRIMARY KEY (ID));

ALTER SEQUENCE MENTOR_ID_seq RESTART WITH 1000;

CREATE TABLE STUDENT (
ID SERIAL,
Name VARCHAR(20) NOT NULL,
PRIMARY KEY (ID));

ALTER SEQUENCE STUDENT_ID_seq RESTART WITH 1000;

CREATE TABLE PARENT (
ID SERIAL,
Name VARCHAR(20) NOT NULL,
PRIMARY KEY (ID));

ALTER SEQUENCE PARENT_ID_seq RESTART WITH 1000;

CREATE TABLE TASK (

)
